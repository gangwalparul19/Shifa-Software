/**
 * Development environment configuration for the Storefront PWA.
 * `apiBaseUrl` points at the local Spring Boot OMS REST API.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
};
