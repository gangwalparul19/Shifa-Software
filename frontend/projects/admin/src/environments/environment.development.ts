/**
 * Development environment configuration for the Admin Dashboard.
 * `apiBaseUrl` points at the local Spring Boot OMS REST API.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
};
