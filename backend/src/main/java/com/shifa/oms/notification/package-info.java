/**
 * Notification service (WhatsApp).
 *
 * <p>Sends pre-approved Meta template messages on lifecycle events via the
 * WhatsApp Business API, driven by the transactional outbox with retries.
 */
package com.shifa.oms.notification;
