/**
 * Authentication and role-based access control (RBAC).
 *
 * <p>Owns JWT issuance/refresh, the {@code User}/role model, BCrypt password
 * hashing, the Spring Security filter chain, and salesperson query scoping.
 */
package com.shifa.oms.auth;
