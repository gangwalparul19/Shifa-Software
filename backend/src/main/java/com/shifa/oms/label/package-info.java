/**
 * Label service.
 *
 * <p>Generates internal company labels (Code128 barcode + order details) and
 * courier shipping labels as single/bulk PDFs, uploaded to Object Storage.
 */
package com.shifa.oms.label;
